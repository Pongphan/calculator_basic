# Basic Button Calculator

A simple Streamlit calculator with a familiar button keypad. It supports the four
basic operations, decimals, percentages, sign changes, clear, and backspace.

## Run locally

```powershell
cd calculator_basic
python -m pip install -r requirements.txt
python -m streamlit run app.py
```
